# ChairUp — fresh start (Next.js 14 + Supabase)
- Relative imports (no alias) so Vercel compiles cleanly.
- Run `supabase.sql` in your Supabase project first.

## Quickstart
npm i
cp .env.example .env.local
# add Supabase URL + anon key
npm run dev
